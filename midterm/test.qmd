---
title: "title"
date: last-modified
author: "Hayden Coke"
format:
  html:
    embed-resources: true
jupyter: python3
---

```{python}
import numpy as np
import pandas as pd
import random
```

1

```{python}
df = pd.DataFrame({
    'A' : [1.2, 3.4, 5.6],
    'B' : [0, 1, 2],
    'C' : ['7', 'B', '2'],
    'D' : [True, False, True]
})

df[['A', 'D']]

```

2

```{python}

sizes = random.sample(range(0, 10), 9)

df = pd.DataFrame({
    'Color' : ['Blue', 'Orange', 'Red', 'Blue', 'Orange', 'Red', 'Blue', 'Orange', 'Red'],
    'Size' : sizes
})

df2 = df[df['Color'] == ("Blue"|"Red") & (df['Size'] < 5 & df['Size'] > 2)]

# new_df = df[(df['Color'] == 'Blue') | (df['Color']=='Red')]
# new_df = new_df[(new_df['Size'] < 5) & (new_df['Size'] > 2)]

```